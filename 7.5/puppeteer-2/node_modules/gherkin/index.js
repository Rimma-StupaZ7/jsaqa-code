(function (factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module
    define([], factory)
  }
  if (typeof module !== 'undefined' && module.exports) {
    // Node.js/RequireJS
    module.exports = factory();
  }
  if (typeof window === 'object'){
    // Browser globals
    window.Gherkin = factory();
  }
}(function () {
  return {
    Parser: require('gherkin/lib/gherkin/parser'),
    TokenScanner: require('gherkin/lib/gherkin/token_scanner'),
    TokenMatcher: require('gherkin/lib/gherkin/token_matcher'),
    AstBuilder: require('gherkin/lib/gherkin/ast_builder'),
    Compiler: require('gherkin/lib/gherkin/pickles/compiler'),
    DIALECTS: require('gherkin/lib/gherkin/dialects'),
    generateEvents: require('gherkin/lib/gherkin/generate_events')
  };
}));
