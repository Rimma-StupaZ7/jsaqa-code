import GroupBuilder from 'cucumber-expressions/dist/src/GroupBuilder';
export default class TreeRegexp {
    regexp: RegExp;
    private regex;
    groupBuilder: GroupBuilder;
    constructor(regexp: RegExp | string);
    match(s: string): import("cucumber-expressions/dist/src/Group").default;
}
